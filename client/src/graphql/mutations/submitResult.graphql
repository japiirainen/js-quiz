mutation SubmitResult($input: ProblemResultInput!) {
   submitResult(input: $input) {
      solution
      success
      errors {
         message
         actual
         expected
      }
      user {
         ...RegUser
      }
   }
}

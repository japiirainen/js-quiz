// import { removeCommentAndWhitespace } from './testRunner'

// test('should remove comments and extra whitespace', () => {
//    const res = removeCommentAndWhitespace(`

//     const sayHi = (msg) => msg`)
//    expect(res).toBe(`const sayHi = (msg) => msg`)
// })

query GetProblemByIndex($index: Int!) {
   getProblemByIndex(index: $index) {
      currProblem {
         ...RegProblem
      }
      prevProblem {
         ...RegProblem
      }
      nextProblem {
         ...RegProblem
      }
   }
}
